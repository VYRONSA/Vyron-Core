<?xml version="1.0" encoding="utf-8"?>
<!--
    HTTPS everywhere, with ONE deliberate exception.

    The debug build talks to a QA server on the local network over http, because
    pointing a test build at production would create real operational records in
    a live tenant. That exception is pinned to the QA hosts by name below — it is
    not a global cleartext permit, so a release build reaching any other host
    still requires TLS and a session cookie can never travel in the clear.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- The QA server on the developer's LAN. -->
        <domain includeSubdomains="false">192.168.101.175</domain>
        <!-- The host machine, as seen from an Android emulator. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
